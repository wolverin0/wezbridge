<!-- doc-head: Four real bounded autonomy jobs and current resumed checkpoint, 2026-09-08; frozen intake and remaining live gates -->
Exact revisions, model phases, accepted lease/RPC/context evidence, live outbox receipt, isolated backup and remaining limits.
Read with CONTROL.md; job acceptance and local proofs remain distinct from protected live intake or production readiness.
<!-- /doc-head -->

# Provider-backed evidence

## Current resumed checkpoint

Four real fictional jobs remain preserved at 10 of 12 aggregate phases, with
ten receipts, four repositories and three held pending notices. The runtime
is stopped: `activeWorkers=0`, no service/plugin, bot consumer, recurring
schedule, protected native owner callback or new model is active.

The accepted native Node gate is 179 passed with one explicit model opt-in skip,
plus check/build. DriverLease has 17 tests and narrow Desktop/VM SQLite
recovery evidence: same/live rejection, stopped recovery, one unique effect,
four events and exact absence of three containers. The Desktop report is
`9A5C...F3F1`; the final VM artifact is
`G:/tmp/autonomy-vm-recovery-final-20260908.json` with identifier
`B3A832...BA01`. This is not full driver, worker, supervisor, systemd or reboot
proof.

The finite Docker RPC has 19 accepted tests and six real model-free cases on a
restored stopped scratch volume, using the local Unix socket, immutable image,
exact existing labeled volume and fixed `/pilot-runtime/state-v2` root. The
original volume was not migrated. The supported restore was a disposable
stopped copy containing ten receipts and four repositories and reporting
`database=ok`.

Callback backend/service coverage has 25 accepted pure-backend cases. A
same-UID model terminal or Docker client can still forge the callback source;
protected native ingress and a reviewed trust-domain change are open. MemoryMaster
has 27 passing tests and three POSIX skips on Windows from 30 collected, with
WSL 30/30. Actual registered Windows MCP-to-Python-wire-to-compiled-JobStore-to-
SQLite intake returned `MISSING` with no eligible claims, one source call across the initial and duplicate submission, zero models and zero workers in
`G:/tmp/autonomy-memory-live-intake-proof-20260908.json`
(`E47E39BB...FDE94`). Positive `READY` usefulness and a configured live source
callback remain unproved; the VM isolated MCP/Pydantic path is `UNAVAILABLE`.
Task-scoped source/storage integration is implemented and accepted. Configuring the bounded live source remains open.

The current accepted local evidence does not authorize Hermes/plugin activation,
owner callbacks, production actions, new notices or service startup.

The private facade-to-Docker health runtime path passes with a synthetic
identity only. Native MemoryMaster context is bound but remains unverified in a
live owner journey.

The scoped Docker runtime used native Codex0.153.4 with its existing supported ChatGPT authentication. A real model canary demonstrated permitted task reads/writes, denied dummy-private read, denied oracle write and network EPERM from the worker tool boundary. Authenticated client network access is separate from worker tool network permission. No authentication was extracted or copied into the image, no Docker socket was mounted, and the container used a read-only root, restricted capabilities, named state volume and scoped seccomp profile. This is bounded observed isolation evidence, not a universal sandbox certification.

The real native verifier first exposed missing profile selection, dotted filesystem override keys and empty stdout under its sandbox. Fixes select the named profile, encode full paths inside one TOML table and use synchronous completion output. Inverse native mutations failed as expected; restored source passed. Protected criteria were not removed. NATIVE-REVIEW.md records the separate read-only review and its limits.

| Fictional task | Result | Model phases | Exact accepted revision |
|---|---|---:|---|
| CRM search | VERIFIED; independent rejection, repair and retest | 4 | 40330a05b4e58c8f8a3bfb893f7f5f8c4e6a551a |
| Infrastructure report | VERIFIED; worker UNKNOWN and backup UNVERIFIED preserved | 2 | 23fa4d1598804558f9ba9afe45d2fed93d786909 |
| Spanish marketing copy | VERIFIED draft; no publication or invented delivery guarantee | 2 | b18fe9ef1d45f45f6df74cc6188fbcd414081d44 |
| Static design review | VERIFIED report; design verdict NEEDS_WORK | 2 | b2924e590251ed27efccc2b2f3cb3711ccda3137 |

CRM ran02:34:16–02:37:33UTC. The other three ran sequentially02:45:35–02:51:04UTC. Ten distinct provider thread completions and separate author/reviewer actors are persisted. CRM deliberately received a controller-injected ID-prefix defect after the initial author proposal; the original and injected hashes were retained, and the independent reviewer was not given the seed. This proves controlled rejection/repair, not a naturally occurring model-authored defect. The driver handled phase transitions without an operator relay during these jobs; overall setup attention and comparative time savings are not measured.

The design run supplied both immutable desktop/mobile PNGs through native CLI image arguments. Its report identifies the pale button/white-text contrast and11px schedule labels and explicitly leaves keyboard, screen reader, focus and dynamic interactions untested. The accepted output is a review that rejects the visual design, not acceptance of the page itself. No redesign was performed.

Reported usage across the ten job completions:734371 input tokens and15567 output tokens; input totals include cached tokens. No monetary cost is derived from subscription usage. This excludes discovery, review agents, earlier canaries and the surrounding implementation session.

# Delivery and stopped state

The CRM terminal outbox event was sent once through a finite trusted host bridge invoking the existing guarded Hermes CLI. The fresh guard checked protected owner configuration and primary private-chat routing. Telegram returned39805; the same event settled SENT at attempt1. A second outbox delivery returned null and did not invoke the sender again. This proves durable settlement for the observed response; ambiguous sends still cannot promise exactly-once behavior. Owner reading/approval remains unknown. The plugin notifier.py and live Hermes intake were not deployed by this test.

At operator pause: stopped=1, activeWorkers=0, no autonomy containers running. The other three terminal notices remain PENDING and held. Accepted source artifacts, reviews, revisions, dispatches and usage were exported to G:/tmp/autonomy-docker-pilot-20260908/accepted-results.json. No new service, plugin, bot consumer or recurring task remains active.

Backup CLI completed successfully with ten receipt artifacts and four repositories. Archive: owned volume autonomy-pilot-state-20260908:/pilot/pause-backup-20260908; manifest SHA256 8431be39cb3612c291bc274792f91454f9ab87da99db526c664328107c4f78ca. This new pause backup has not yet undergone its own restore rehearsal; prior disposable restore tests passed.

# Resume boundary

Accepted bounded commits now include `ba8c4ec` (native isolation), `76f706f` (decisions), `1a64e4c` (lease), `4d79e00` (Docker RPC) and `73b6db3` (helper preflight); MemoryMaster retrieval and frozen intake are committed as `25dd84e`. Production/shared checkouts were preserved. The dated unaccepted Docker recovery drafts remain outside the native worktree under recovery-wip as failure evidence. Required follow-up: full supervised child-stop/takeover proof, bounded live memory-source wiring, protected Hermes intake/control and real owner callbacks, then a bounded elapsed observation window. Do not resume execution or notification delivery without the operator resuming the paused mission.
