<!-- doc-head: T-0418 current resumed checkpoint, 2026-09-08; frozen intake and remaining live gates -->
Single mission checklist and continuation for the paused four-job pilot.
Accepted evidence now covers Node, narrow DriverLease, finite Docker RPC, pure callbacks and MemoryMaster parser/native plus registered-intake checks.
Read MODEL-PILOTS.md and EVIDENCE.md; positive memory usefulness, protected ingress, supervisor recovery, owner flow and soak remain open.
<!-- /doc-head -->

# Autonomy mission control

Source: https://chatgpt.com/c/6a9f146c-8e4c-83e9-a35b-69da3eb0e7aa. Full CODEX_AUTONOMY_MISSION.md and CODEX_START_PROMPT.txt read in the signed-in browser. Fleet card T-0418; correlation autonomy-mission-20260907. This is the single mission backlog. The operator subsequently said “continue”; the original mission and boundaries remain active.

## Outcome and authority

Reduce coordination per verified outcome through one conversation, durable ownership, scoped context, replaceable specialists, bounded execution, independent review, repair and useful delivery. The mission is not complete.

Authorized: isolated development worktrees, code/tests/docs, local commits, bounded provider pilots and independent review. One harmless Telegram test was sent to the verified owner. Production/network/security/account changes, purchases, customer messaging, publication, route replacement and disruptive shared-host restarts need applicable explicit authority. No new bot, duplicate consumer or recurring workload was created.

## Locations

- Native implementation: G:/tmp/finalorchestra-autonomy-20260907; feat/autonomy-pilot-20260907; base ad2794671b00a1e499c9ecad3ef025d56078cb03.
- Mission reports: G:/tmp/wezbridge-autonomy-20260907; same branch name; base 09084b6e53cc92f5d2f9753e097ec3cefe226bfa.
- Linux copy: ~/autonomy-pilot-20260907/engine in Ubuntu WSL; final code copied and full Node suite passed.
- Shared dirty checkouts, installed Eve/Postgres, adult/child Hermes and schedules remain preserved.
- Native docs/AUTONOMY-PILOT.md is the operating interface; integrations/hermes-autonomy/README.md contains staging/rollback. Neither is an active installation.

## Checklist

Resume2026-09-08: operator explicitly requested parallel agents chosen by complexity to avoid inherited maximum-model/effort cost. The first wave is exactly three childless agents with fork_turns=none: resume_recovery uses gpt-5.6-terra/high for kernel process ownership; resume_memory uses gpt-5.6-luna/high for the bounded existing-API adapter; resume_review uses gpt-5.6-sol/high for independent security/acceptance judgment. Each has distinct file ownership and a short task brief. No max/ultra or fast profile is used for them. Parent owns integration/runtime and does not duplicate their assigned implementation. Escalate only for a concrete blocker, not by default. Completed four model jobs will not be rerun; deterministic recovery probes use no model. Persisted twelve-phase job limit remains unchanged.

Native worktree/paused Docker image and volume were verified intact. A read-only probe found existing Docker29.1.3 on the Hermes VM; a disposable same-host runtime test may avoid the unworking VM-to-Windows SSH route. No account/key/network changes are authorized by that discovery. Held outcomes remain held during preflight.

- [x] Read full handoff; establish Goal, isolated worktrees and one Fleet card.
- [x] Inventory real source/ref/dirty state, installs, processes, startup, dependencies and known hosts.
- [x] Map Telegram consumer/owner and existing MemoryMaster integration.
- [x] Trace retained real R5 recovery/provider failure/cancellation; no accepted outcome established.
- [x] Inspect analytics and minimal curated history; intervention rates UNMEASURED.
- [x] Select and independently review minimal architecture; preserve alternatives.
- [x] Map domain/project/capability distinctions and existing native specialists; no bulk conversion.
- [x] Implement origin-bound intake, one task owner, bounded driver, fenced execution, exact-revision review, repair and persisted outcome.
- [x] Implement responsive finite status/evidence/health/decisions/pause/resume/cancel RPC and emergency stop.
- [x] Test one-use local decisions: wrong owner, denial, expiry, replay, changed artifact/scope.
- [x] Test duplicates, stale/out-of-order events, aborts, lease expiry/replacement, provider errors and uncertain delivery.
- [x] Run real Git/SQLite scripted rejection -> repair -> retest -> VERIFIED: four dispatches, two revisions, one terminal notice.
- [x] Render and inspect synthetic desktop/mobile design; bind hashes. No design acceptance claimed.
- [x] Test coherent backup and stopped isolated restore; prevent historical notice replay after enable.
- [x] Verify one harmless existing Hermes send, receipt39790; owner reading unknown.
- [x] Complete bounded independent adversarial review; fix findings and rerun regressions.
- [x] Final Node gate:179 pass + one explicit model opt-in skip; check/build pass.
- [x] Applicable Python adapter cases and offline systemd syntax pass.
- [x] Record operating guide, review, measurements, rollback and continuation.
- [x] Complete bounded authenticated worker/tool isolation in the scoped Docker host; retain earlier failures.
- [x] Run four provider-backed jobs: real independent CRM rejection/repair, infra report, marketing draft and image-supplied design review. See proof limits in MODEL-PILOTS.md.
- [x] Current narrow lease evidence: 17 tests, Desktop/VM SQLite recovery, one effect, four events and exact absence of three containers.
- [x] Current finite RPC evidence: 19 accepted tests and six real model-free cases on a restored stopped scratch volume.
- [x] Current pure callback evidence: 25 accepted cases across callback backend and service behavior; protected native ingress remains absent.
- [x] Current MemoryMaster evidence: 27 tests passed and three POSIX cases skipped on Windows from 30 collected; WSL passed 30/30.
- [x] Current registered Windows MCP-to-JobStore-to-SQLite intake: `MISSING`, no eligible claims, one source call across the initial and duplicate submission, zero models/workers; `autonomy-memory-live-intake-proof-20260908.json` (`E47E39BB...FDE94`).
- [x] Integrate protected per-job memory snapshots: ten SQLite/contract tests cover binding, facets, conflicts, missing facts, rollback and immutable replay.
- [ ] Configure the bounded live memory source and prove positive `READY` usefulness; callback deadline and VM runtime remain open.
- [ ] Add the protected native ingress and verify live owner intake/control; same-UID model-terminal/Docker callbacks remain forgeable.
- [ ] Activate/verify bounded supervisor: start, responsive RPC, kill/recover, restart, stop and process group death.
- [ ] Observe agreed pilot window and measure real outcomes, interventions, usage/cost and notices.
- [ ] Accept cutover only after mandatory gates. No production-readiness claim.

## Current resumed checkpoint, 2026-09-08

The runtime remains stopped with `activeWorkers=0`. Four real fictional jobs,
ten receipts, four repositories and three held pending notices are preserved
at the 10/12 aggregate phase budget. No service, plugin, bot consumer, recurring
schedule, new model, owner callback or production action is active.

The native Node gate is 179 passed, one explicit model opt-in skip, with check
and build passed. DriverLease evidence is 17 tests plus the Desktop report
`9A5C...F3F1` and final VM artifact
`G:/tmp/autonomy-vm-recovery-final-20260908.json` (`B3A832...BA01`). It proves
same/live rejection, stopped recovery, one SQLite effect, four events and exact
absence of three containers; it remains a narrow guard/harness proof.

The finite Docker RPC has 19 accepted tests and six real model-free cases on a
restored stopped scratch volume, using the local Unix socket, immutable image,
exact labeled existing volume and fixed `/pilot-runtime/state-v2` root. The
original volume was not migrated; the disposable supported restore held ten
receipts and four repositories and reported `database=ok`. Callback
backend/service coverage has 25 accepted cases. MemoryMaster has 27 passing
tests and three POSIX skips on Windows; its registered Windows intake proof is
accepted for `MISSING` with no eligible claims. Positive `READY` usefulness and
a configured live source remain unproved. The protected per-intake snapshot is implemented and independently accepted. The default VM Python
path is `UNAVAILABLE` because isolated MCP/Pydantic dependencies are missing;
the existing Hermes venv lacks `mcp.server.fastmcp`. Both paths remain
`UNAVAILABLE`, and no install/configuration change was made.

The same-UID model-terminal/Docker path can forge callback provenance. A
separately reviewed trust-domain change and protected native ingress are still
required before owner control or approval can be considered.

Current implementation is committed through `25dd84e` in the native worktree.
Independent acceptance and exact hashes are in [RESUME-REVIEW.md](RESUME-REVIEW.md).
The final source-volume check used a read-only mount and an unchanged tmpfs
copy: stopped=1, zero active workers, four VERIFIED jobs, ten phases, three
PENDING notices and one SENT notice, integrity ok, exact probe container absent.
Evidence: `G:/tmp/autonomy-original-volume-final-status-copy-20260908.json`.

## Operator pause and next action, 2026-09-08 02:54 UTC

The operator requested a graceful pause. All four real jobs finished before shutdown. The private state at Docker volume autonomy-pilot-state-20260908:/pilot/state-v2 has stopped=1, activeWorkers=0, four VERIFIED jobs and ten completed model dispatches. CRM notification is SENT with receipt39805; three other notices remain PENDING and delivery is held. No autonomy container is running. Existing Hermes, other containers, schedules and customer systems were preserved.

Fresh consistent backup: /pilot/pause-backup-20260908 in the same owned volume, ten receipt artifacts and four Git bundles, manifest SHA256 8431be39cb3612c291bc274792f91454f9ab87da99db526c664328107c4f78ca. Host evidence and scripts: G:/tmp/autonomy-docker-pilot-20260908. No token or home-directory data is in the backup.

WSL provider connectivity still fails; the operator authorized its recovery attempt and Ubuntu was already stopped, so it was started without terminating another session. Scoped Docker solved provider execution without changing global network/security settings. The failed Windows private-read and server namespace probes remain rejected alternatives.

Current accepted commits are `ba8c4ec` (native isolation), `76f706f` (decisions), `1a64e4c` (lease), `4d79e00` (Docker RPC) and `73b6db3` (helper preflight); the final MemoryMaster source commit remains pending root closeout. Earlier uncommitted source/package/script changes and their 127-test regression evidence remain historical artifacts and are not a release or activation decision.

The dated Docker crash-recovery draft was not accepted: three of seven draft tests failed on Windows path mocking and no actual kill/recover proof ran. It remains preserved as failure evidence under `G:/tmp/autonomy-docker-pilot-20260908/recovery-wip`; the later accepted narrow lease and finite-RPC artifacts are recorded in the current checkpoint above. Do not enable workers or drain held notices merely because a new session opens. Live plugin, recurring service, production cutover and soak remain unproved.

## Reports

ENGINE-INVENTORY.md and HERMES-INVENTORY.md: exact source/install/runtime and owner evidence. CONTEXT-INVENTORY.md and SPECIALISTS.md: analytics/native contracts. ARCHITECTURE.md and ARCHITECTURE-REVIEW.md: choice and initial review. ISOLATION-SPIKE.md: command probes/corrected constraints. CORE-REVIEW.md: red findings and repaired checks. EVIDENCE.md: final acceptance matrix and measurement limits.
