# T-0596 PASO 2 ITEMS 4-5 — retire WezTerm as delivery transport + docs

Brief + log del worker T3. Rama `fix/t0596-items45-wezterm-docs` desde
`origin/fix/t0596-paso2-orca-unico` (head 3335b9e, PR #55 aun abierto al momento de arrancar —
`gh pr view 55` = `state: OPEN`). PR abierta contra base `fix/t0596-paso2-orca-unico`; se
retargetea a `main` cuando #55 mergea. Lee tambien `_intel/briefs/2026-09-24-T0596-paso2.md`
(items 1-3: self-send guard, queue-drain via Orca, scripts Python via a2a_send).

## Contexto (decision operador/Fleet 24/09)
La flota vive en terminales Orca; WezTerm tiene 0 panes. Orca es el UNICO transporte, `a2a_send`
el UNICO camino de envio, el ledger sigue siendo la unica autoridad.

## Item 4 — retirar WezTerm como transporte, detras de flag

**Diagnostico previo:** antes de este item, `a2a_send`'s `to_project` resolution y
`project-queue.cjs`'s `findTarget` intentaban WezTerm PRIMERO (pane-identity's `resolve`) y solo
caian a Orca cuando WezTerm no encontraba nada usable — precedencia WezTerm incondicional,
comentada explicitamente asi en el codigo (`// WezTerm keeps unconditional precedence above`).
Eso contradice la decision del operador: con la flota en Orca, WezTerm nunca deberia intentarse
primero por default.

**Cambio (menor diff elegido, no se borro el branch):** en los DOS call sites
(`src/mcp-server.cjs`'s `a2a_send` y `src/project-queue.cjs`'s `findTarget`), la resolucion via
pane WezTerm ahora queda detras de `WEZBRIDGE_WEZTERM_TRANSPORT === '1'` (default: no seteada =
off). Con el flag off, `hit` se fuerza a `{ paneId: null, ambiguous: [], warning: null }` SIN
siquiera llamar `pane-identity.resolve` — Orca se intenta primero, siempre. Con el flag on, el
comportamiento previo (WezTerm primero, Orca como fallback) queda intacto byte a byte.

**Por que flag y no borrar el branch:** (1) diff minimo — un `if` por call site en vez de mover
~150 lineas de logica de entrega WezTerm y sus tests; (2) preserva la cobertura EXISTENTE del
camino WezTerm (util si un pane WezTerm real vuelve algun dia) simplemente seteando el flag en
el setup de esos tests, en vez de borrarla; (3) el comportamiento efectivo por default es
identico a "removido" (WezTerm nunca se intenta) sin el riesgo de un refactor mas grande.

**Un target que solo resuelve via WezTerm:** con el flag off, nunca llega a intentarse esa
resolucion — el flujo cae directo a la rama Orca existente (item 1-3), que si no encuentra
terminal Orca tampoco, ENCOLA (`a2a_send`) o DROPEA con motivo (`deliverPending` -> `dropEntry`
`'project-not-live'`), nunca silenciosamente. Cubierto por
`test/wezterm-transport-flag.test.cjs`'s tercer subtest.

**send_prompt:** descripcion del tool actualizada — marcada deprecated para mensajeria de flota
("use a2a_send"), documentando por que (`send_prompt` no aplica gate/shape/lease/cola/audit).
Sigue siendo un tool WezTerm-only (no se toco su implementacion). Lista de tools que siguen
siendo WezTerm-only (verificada leyendo `TOOLS` en `src/mcp-server.cjs`): `discover_sessions`,
`send_prompt`, `read_output`, `send_key`, `get_status`, `list_projects`, `kill_session`,
`set_tab_title`. Retirarlos es una carta aparte, no tocada aca.

### Evidencia fail-first
`test/wezterm-transport-flag.test.cjs` (nuevo, 5 subtests): con el codigo ANTERIOR al fix
(`git stash` solo de `src/mcp-server.cjs` + `src/project-queue.cjs`, confirmado restaurado
despues), 3/5 subtests en ROJO —
- "queue-drain: flag OFF ... never used; Orca resolved first" — `wezCalls.length` 1 != 0
  esperado (WezTerm SI se usaba pese al flag off).
- "queue-drain: flag OFF and no live Orca terminal either — ... never silently dropped" —
  `dropped` 0 != 1 (con WezTerm resolviendo el pane, el mensaje se ENTREGABA via WezTerm en vez
  de dropearse/encolarse, que es justo el bug que el flag corrige).
- "a2a_send: flag OFF ... 0 wezterm calls" — `wezCalls` 1 != 0.
Restaurado el fix (`git stash apply` + `git stash drop`, nunca `pop`, sha verificada antes y
despues): **5/5 verde**.

### Regresiones (existing WezTerm coverage, flag on donde corresponde)
`test/project-queue.test.cjs` (41 tests, ejercita `sendPromptDeferredEnter`/`IDLE_PANE` end to
end) y `test/mcp-queue-sender-project.test.cjs` (5 tests, spawnea `mcp-server.cjs` real con un
census WezTerm mockeado) rompian con el flag off por default — se les agrego
`WEZBRIDGE_WEZTERM_TRANSPORT=1` (top-of-file para el primero, env del child spawneado para el
segundo) para que sigan probando el camino legado, flag on. Confirmado contra baseline (mismo
commit sin mis cambios, via stash) que las 6 fallas restantes en el batch mas amplio corrido
(`call-site gate: mcp-server persists...` x3, `pinned identity rejection...`,
`send_prompt bloquea un sobre a mano...`, `a2a_send nombra los pass sin artefacto...`) son
PRE-EXISTENTES, no causadas por este item — corren igual en el commit base.

### Commit
`feat(a2a-transport): retire WezTerm as default delivery transport (T-0596 item 4)` (27e4aae).

## Item 5 — docs

Actualizados con cabecera de 7 lineas vigente en los tres:
- `docs/a2a-protocol.md`: nueva seccion "Transport: Orca vs. WezTerm (T-0596, 2026-09-24)" antes
  de "## Sending" (resolver compartido, entrega, self-send guard, backlog seal, CLI no-MCP, flag
  legado + lista de tools WezTerm-only); fila "Durable project addressing" de la tabla de
  enforcement actualizada para nombrar Orca como transporte primario. doc-head (lineas 1-7)
  actualizado con T-0596.
- `docs/operations.md`: nueva seccion "## Transporte Orca — a2a_send y queue-drain (T-0596,
  2026-09-24)" (resolver, entrega, self-send guard, backlog seal, CLI, flag legado, regla dura
  de nunca smoke-testear contra el propio pane) insertada antes de "## Mux-wedge". Blockquote de
  cabecera (Que cubre/Leer cuando/Terminos clave) actualizado con los terminos nuevos. Nota de
  flake agregada junto a la medicion S6 existente: `fleet-digest.test.cjs` AC4 necesita un
  `_intel/pane-events.jsonl` real del checkout del operador — no reproduce limpio en un worktree
  aislado; referencia cruzada a los demas flakes de worktree conocidos (lane-hooks p95,
  model-tiers ENOENT, daemon-cli, mcp-server timeouts, tasks-watcher). No existia una lista
  centralizada de "known flakes" en `docs/` antes de esta carta — se documento en el lugar mas
  natural (junto a la medicion S6 que ya discutia ese mismo test/AC).
- `docs/repo-layout.md`: bloque "Post-v3.3" ampliado con una entrada dedicada "Transporte Orca
  (T-0596, ...)" listando `orca-census.cjs`, `orca-target.cjs`, `orca-send.cjs`,
  `bin/a2a-send-cli.cjs`, y notando que el transporte WezTerm de `a2a_send`/`project-queue.cjs`
  es legado apagado por default. `orchestrator-waker.cjs` anotado como WezTerm-only/desarmado
  (ya lo decia operations.md, faltaba en el mapa de modulos). Cabecera recortada de vuelta a 7
  lineas tras la primera edicion (se paso a 8, corregido).

### Commit
`docs(a2a-orca): document Orca transport, WEZBRIDGE_WEZTERM_TRANSPORT legacy flag (T-0596 item 5)`
(6aeaf51).

## Suite completa (una sola corrida, al cierre, sin `WEZBRIDGE_COMPANIONS_DIR`)
Ver seccion final del reporte de cierre (WORKER_DONE) para los numeros — corridos DESPUES de
escribir este brief, siguiendo la regla de "una sola corrida al final".

## Reglas duras respetadas
- Cero sends en vivo contra un pane/terminal real — todo con dobles (`sendPromptDeferredEnter`
  inyectado, `resolveOrcaTarget`/`orcaSend` inyectados, preload de subprocess que stubea
  `pane-discovery.cjs`/`orca-target.cjs`/`verified-send.cjs`).
- `git stash` nunca bare: siempre `git stash push -u -m "<tag-unico>"`, sha capturada de
  inmediato con `git stash list --format='%H %gs'`, restaurado con `apply <sha>` (nunca `pop`),
  dropeado (`git stash drop stash@{0}`) recien despues de confirmar `git status` limpio. Usado
  dos veces: (1) para confirmar que las 6 fallas del batch amplio son baseline, no mias;
  (2) para demostrar rojo->verde de `wezterm-transport-flag.test.cjs` contra el codigo pre-fix.
- No se toco `main`; PR contra `fix/t0596-paso2-orca-unico` (stacked sobre #55, se retargetea a
  `main` cuando esa mergea), nunca merge propio.
- Paths tocados: `src/**`, `test/**`, `docs/**`, `_intel/briefs/**` — dentro de lo permitido.
