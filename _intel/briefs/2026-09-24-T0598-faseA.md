# T-0598 Fase A — automation → ledger router (plan + evidencia)

<!-- doc-head: brief de Fase A del contrato automation->carril (T-0598). Cubre inventario de
     automatizaciones, contrato JSON de findings, scripts/automation-router.cjs, tests fail-first
     y el plan de Fase B (migrar curador WISP / DaemonSentinel / gmail-recordatorios). Leer antes
     de tocar scripts/automation-router.cjs o docs/operations.md sección "Automation router". -->

## Prompt recibido (resumen)
Regla del operador 24/09: toda tarea programada que produce algo accionable debe entregarlo
como tarjeta del ledger + a2a_send al carril dueño, nunca como log o sesión interactiva viva.
Fase A = código + docs + tests, SIN corridas reales, SIN tocar Task Scheduler/VM/Hermes, SIN
tocar las automatizaciones reales. Alcance: inventario, contrato de findings JSON, router
`scripts/automation-router.cjs`, tests fail-first (6 casos), helper `emit-finding.cjs`, y el
plan de Fase B por escrito (sin ejecutarlo).

## Gap encontrado antes de empezar (documentado, no bloqueante)
El brief y `_intel/tasks/T-0598.json` (`context_refs`) apuntan a
`wezbridge/_intel/briefs/audit-automations-2026-09-22/{A-local,B-vm,C-hermes}.md`. Esos archivos
NO existen en ningún branch (`git log --all --diff-filter=A -- "*audit-automations*"` vacío) ni
en el worktree. En vez de bloquear la Fase A por un insumo faltante, reconstruí el inventario de
fuentes primarias que sí existen:
- `Py Apps/_intel/briefs/bots-inventario-20260902.md` — inventario medido de hidden-tasks/schtasks
  de esta PC + crons de Hermes desktop (fecha 2026-09-02, pre-existente, no producido por mí).
- `Py Apps/_intel/briefs/handoff-wisp-worker-2026-09-23.md` — curador nocturno WISP en la VM
  (`~/logs/wisp-nocturnal-rf-sweep.log`, disparo 03:15 ART) y `winback_watcher.py` (cron cada
  minuto en la VM).
- `wezbridge/src/gmail-routine-*.cjs` + `test/gmail-routine-*.test.cjs` — gmail-recordatorios ya
  vive en este repo (T-0339, headless).
- `wezbridge/scripts/routine-registry.cjs` / `routine-audit.cjs` — mecanismo C del contrato de
  bots ya vigente en wezbridge (rutinas que escriben a `_intel/routine-findings/`).
Todas las filas marcadas `inferred` abajo son las que no tengo medición directa propia; están
marcadas así en vez de presentadas como medidas.

## AC1 — Inventario (ver `docs/operations.md`, sección "Automation → ledger router (T-0598)")
Tabla: automation | dónde corre | trigger | salida hoy | ¿accionable? | repo dueño (inferred si aplica).

## AC2 — Contrato de findings JSON
`_intel/automation-findings/<task>-<YYYYMMDD[-HHMM]>.json`:
```json
{ "task": "string", "repo_owner": "string", "actionable": true, "summary": "string",
  "evidence": "string", "severity": "low|medium|high|critical", "fingerprint": "string (opcional)",
  "kind": "string (opcional, default general)" }
```
Fingerprint: si `fingerprint` viene explícito se usa tal cual; si no, se deriva como
`sha256(task + "|" + normalize(summary)).slice(0,16)` con `normalize` = trim + lowercase +
colapsar espacios. Documentado y validado en `src/automation-finding-schema.cjs`.

## AC3 — Router `scripts/automation-router.cjs`
- Escanea `_intel/automation-findings/*.json` (o `--file <path>` puntual).
- JSON inválido / falta `repo_owner` (u otro campo requerido) → cuarentena
  (`_intel/automation-findings/quarantine/`) + log (`router-log.jsonl`), nunca tira el proceso.
- `actionable:false` → solo log (`non-actionable.jsonl`), sin tarjeta, sin send; archivo movido a
  `processed/`.
- `actionable:true` → UNA tarjeta ready en el repo dueño vía `_docs-curation/ledger.cjs create
  --origin <fingerprint>` (ledger YA es idempotente por `origin_key` — reimportar el mismo
  fingerprint devuelve la tarjeta existente en vez de duplicar) + `a2a_send` al carril dueño vía
  `bin/a2a-send-cli.cjs` con `--corr <cardId>`. Estado de "delivered" por fingerprint se persiste
  en `_intel/automation-findings/.router-state.json` para que un fallo de a2a NO duplique la
  tarjeta en la corrida siguiente (reintenta solo el send). Archivo se mueve a `processed/` recién
  cuando delivered:true.
- Ledger CLI y a2a CLI son inyectables (`--ledger-cli`, `--a2a-cli`, o env
  `WEZBRIDGE_AUTOMATION_LEDGER_CLI` / `WEZBRIDGE_AUTOMATION_A2A_CLI`) para que los tests usen
  dobles y NUNCA toquen el ledger real ni manden a2a reales.

## AC4 — helper `bin/emit-finding.cjs`
`node bin/emit-finding.cjs --task X --repo Y --actionable true --summary "..." --evidence "..."
[--severity high] [--kind ...] [--fingerprint ...] [--out-dir <dir>]` escribe un finding válido
con el nombre `<task>-<timestamp>.json`. Un solo one-liner para que Fase B migre cada automatización.

## Tests (fail-first)
`test/automation-router.test.cjs`, 6 casos exigidos por el brief:
1. actionable → 1 tarjeta ready (owner repo) + 1 a2a send delivered:true (doble).
2. mismo finding 2 corridas → sigue 1 sola tarjeta.
3. no actionable → 0 tarjetas, 0 sends, 1 línea de log.
4. archivo inválido → cuarentena.
5. falta `repo_owner` → cuarentena.
6. falla el a2a → tarjeta igual creada, reintenta el send la corrida siguiente, sin tarjeta duplicada.

## Fase B (plan, NO ejecutado en esta Fase A)
| Automatización | Dónde vive hoy | Cambio exacto | Cómo probarlo | Riesgo |
|---|---|---|---|---|
| Curador nocturno WISP | VM, `~/logs/wisp-nocturnal-rf-sweep.log`, cron 03:15 ART, sesión interactiva que queda viva 8h | Migrar a `claude -p` (turno headless, termina solo) que al final llama `emit-finding.cjs` con el hallazgo del sweep y sale; agregar el router a un cron de la VM cada 15 min | Corrida real → tarjeta aparece en whatsappbot-final con evidencia del sweep + a2a delivered | Curador toca RF/UISP de clientes reales; si el finding mal formado queda en cuarentena silenciosa nadie se entera — habría que además loguear cuarentena a Telegram en Fase B |
| DaemonSentinel | PC, schtask `WezBridge-DaemonSentinel`, node, cada 5 min | Cuando detecta el daemon caído, en vez de solo tocar `deadman-touch.json` también emite un finding (`actionable:true`, repo wezbridge) y deja que el router cree la tarjeta | Matar el daemon a propósito, esperar 5 min, ver tarjeta en wezbridge | Falso positivo en un blip transitorio generaría tarjetas ruido — necesita threshold antes de emitir |
| gmail-recordatorios | wezbridge, `src/gmail-routine-*.cjs`, headless (T-0339) | Al final del run headless, emitir finding con el resultado (recordatorio enviado / fallido) | Corrida real → tarjeta en el repo dueño del contacto | **gmail-recordatorios está bajo observación de T-0339 con corridas reales programadas 25/09 y 26/09 08:30 ART — migrarlo antes del 26/09 podría confundir esa observación.** Recomendado: NO tocar gmail-recordatorios hasta después del 26/09 08:30 ART. |

## Reglas seguidas
- Paths tocados: `src/**` `test/**` `scripts/**` `docs/**` `bin/**` + este brief.
- Sin `git stash`. Sin tocar main. Sin sends/cards reales (todo por doble/mock).
- `npm test` completo una sola vez al cierre, sin `WEZBRIDGE_COMPANIONS_DIR`.
